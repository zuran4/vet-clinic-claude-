# A1 — Ρύθμιση Playwright για scraping (vet-api)

Στόχος αυτού του βήματος:

- Να εγκαταστήσουμε το Playwright στο backend (`vet-api`).
- Να ελέγξουμε ότι μπορούμε να ανοίξουμε έναν απλό browser (headless) με Node.
- Να μην πειράξουμε ακόμη κανένα άλλο αρχείο του project.

---

## 1. Πού τρέχουμε τις εντολές

Θέλουμε να βεβαιωθούμε ότι βρισκόμαστε μέσα στο backend του Vet project.

Στο PowerShell:

```bash
cd C:\Users\zuran\Documents\GitHub\Vet-project2\vet-api
